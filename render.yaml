services:
  - type: web
    name: lightning-dragon-tiger
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20.18.1
      - key: ADMIN_PASSWORD
        value: "8959"
